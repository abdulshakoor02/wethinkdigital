# Taste

- When a task is blocked by a missing required tool or dependency (e.g., a CLI like `agent-browser`), prefers the agent to install it and continue rather than stop and report the task as blocked. Confidence: 0.85
